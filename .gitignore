node_modules/
test-results/
playwright-report/
playwright/.auth/
.env
*.log
demo-app/shop.db
rwa-app/
rwa-app-ci/
# Claude Code: keep the shared project settings, ignore everything personal/generated
# (settings.local.json, installed skills/commands, caches). `.claude/*` rather than
# `.claude/` so the re-include below can work — git cannot un-ignore inside an ignored dir.
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/commands/
!.claude/agents/
!.claude/rules/
!.claude/hooks/

# Python (claude-agent demo)
__pycache__/
*.pyc

# PromptLens / SDLC observer — local-only setup notes and artifacts
docs/promptlens-setup.md
*.whl

# claude-config demo artefacts
claude-config/proxy_demo.log

# scratch dirs made by sessions/ demos
.scratch/

# session notes are local study material — only the root README.md is tracked
sessions/**/*.md
